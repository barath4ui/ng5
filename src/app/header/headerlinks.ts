export enum ICurrentitem{
    home = "home",
    about = "about",
    contact = "contact"
}

export enum ICurrentitemData{
    "/" = ICurrentitem.home,
    "/about" = ICurrentitem.about,
    "/contact" = ICurrentitem.contact
}